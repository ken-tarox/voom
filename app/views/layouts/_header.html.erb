<header class="navbar navbar-fixed-top ">
  <div class="container">

    <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
  <% if logged_in? %>
    <%= link_to "ONSENO", current_user, id: "logo", class: "logo-header" %>
    <% else %>
<%= link_to "ONSENO", root_path, id: "logo" %>
<% end %>
    </div>

    <nav>
      <ul id="navbar" class="nav navbar-nav  navbar-right navbar-collapse collapse">
        <% if current_user && current_user.admin? %>
        <li class="user-manage"><span><%= link_to "ユーザー管理", users_path %></span></li>
        <% end %>
        <li><%= link_to "温泉を探す", onsenposts_path %></li>
        <% if logged_in? %>
          <li><%= link_to "MY温泉登録", root_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              アカウント <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><span><%= link_to "プロフィール", current_user %></span></li>
              <li><span><%= link_to "プロフィールを更新", edit_user_path(current_user) %></span></li>
              <li><span><%= link_to "ログアウト", logout_path, method: :delete %></span></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "ユーザー登録", signup_path %></li>
          <li><%= link_to "ログイン", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
